#include "../Model/Entity.h"
#include "../Model/Level.h"

class LevelController{
	Level level;
		
	public:
		/*	

		*/
		/*
		 * Default Constructor
		 */
		GameController();

		/*
		 * Runs the current level.
		 */
		run();


		createLevel();
		createLevel(std::string file);
};
